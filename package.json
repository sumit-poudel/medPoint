{
  "name": "server",
  "version": "1.0.0",
  "description": "server for medpoint",
  "license": "ISC",
  "author": "sumit",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "server": "nodemon ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
