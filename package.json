{
  "name": "server",
  "version": "1.0.0",
  "description": "server for medpoint",
  "license": "ISC",
  "author": "sumit",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "watch": "npx @tailwindcss/cli -i ./tailwind.css -o ./public/style.css --watch",
    "build": "npx @tailwindcss/cli -i ./tailwind.css -o ./public/style.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.12",
    "nodemon": "^3.1.10",
    "tailwindcss": "^4.1.12"
  }
}
